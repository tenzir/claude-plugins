# Key:Value object

A generic object allowing to define a `{key:value}` pair.

## Attributes

| Attribute | Type | Requirement |
|-----------|------|-------------|
| `name` | string_t | required |
| `value` | string_t | recommended |
| `values` | string_t | recommended |

## Constraints

At least one of: `value`, `values`
